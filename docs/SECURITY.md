# Security

> fortuneteller MCP 서버 보안 고려사항

## 위협 모델

이 프로젝트는 사주 분석 MCP 서버로, 민감한 개인정보(생년월일시, 성별, 출생지)를 처리한다.

### 처리하는 민감 데이터

| 데이터 | 민감도 | 저장 여부 |
|--------|--------|-----------|
| 생년월일시 | 중 | 미저장 (메모리 캐시만, 세션 종료 시 소멸) |
| 성별 | 저 | 미저장 |
| 출생지 | 저 | 미저장 |
| 이름 | 중 | 미처리 (도구에 전달하지 않음) |

## 입력 검증

### Zod 스키마 검증

모든 MCP 도구 입력은 `src/schemas/index.ts`의 Zod 스키마로 검증된다:

- 날짜 형식: `YYYY-MM-DD` 패턴 매칭
- 시간 형식: `HH:mm` 패턴 매칭
- 달력 타입: `'solar' | 'lunar'` 리터럴
- 성별: `'male' | 'female'` 리터럴
- 숫자 범위: 월 1-12, 시 0-23

### 문자열 처리

- 입력 문자열은 구조화된 포맷만 허용 (자유 텍스트 입력 없음)
- SQL 인젝션 해당 없음 (DB 미사용)
- XSS 해당 없음 (HTML 렌더링 없음)

## 데이터 보호

### 저장하지 않는 것

- 사용자 개인정보를 파일시스템, DB, 외부 서비스에 저장하지 않음
- 로그에 개인정보를 기록하지 않음
- 외부 API로 개인정보를 전송하지 않음

### 메모리 캐시

- `performance_cache.ts`: 계산 결과를 메모리에 캐싱
- 서버 프로세스 종료 시 모든 캐시 소멸
- 캐시 키에 개인 식별 가능 정보 포함 (birthDate, birthTime, gender)
- **개선 필요**: 캐시 TTL 미설정 -> 장기 실행 서버에서 무한 성장 가능

## 네트워크 보안

### 외부 통신

- 외부 API 호출: **없음**
- 외부 데이터 수신: **없음**
- 모든 데이터 로컬 테이블 기반

### HTTP 서버 (server-http.ts)

- CORS: `*` (모든 출처 허용) -> 프로덕션 배포 시 제한 검토
- SSE 세션 관리: sessionId 기반
- 인증: 미구현 (MCP 프로토콜 수준에서 처리 위임)

## 의존성 보안

### 직접 의존성 (4개)

| 패키지 | 위험도 | 비고 |
|--------|--------|------|
| `@modelcontextprotocol/sdk` | 저 | Anthropic 공식 |
| `@smithery/sdk` | 저 | 배포 통합 |
| `date-fns` / `date-fns-tz` | 저 | 널리 사용되는 날짜 라이브러리 |
| `zod` | 저 | 검증 라이브러리 |

### 권장 사항

- 정기적으로 `npm audit` 실행
- 의존성 업데이트 시 `npm test` + `npm run build` 확인

## 보안 개선 과제

1. **[중간]** HTTP 서버 CORS 정책 제한 (프로덕션 배포 시)
2. **[낮음]** 메모리 캐시 TTL 설정 (장기 실행 방지)
3. **[낮음]** 캐시 키에서 개인 식별 정보 해싱 검토

## 영업 비밀 / 알고리즘 보호 정책 (무료 사주 확장 PR 이후)

도화선의 실질 차별점은 **캐릭터별(doyoon/yeonwoo) Claude 해석**(PR3)이며, 명리 표준 계산 결과(사주 명식·오행 분포·십성·십이운성·신살·용신 오행·신강신약 라벨·대운)는 공개 명리서/오픈소스(`sajuapi`, `pycheonmun` 등)에 이미 공개된 지식이다. 따라서 **기초 데이터는 공개하고, 판정 근거·해석 텍스트·유파 가중치는 보호**한다.

### 공개 OK (API 응답 노출 허용)

- 4기둥 천간·지지 (한자/한글)
- 천간 음양, 지지 음양
- 오행 개수(`wuxing.counts`), 비율(`wuxing.ratios`), 과다/결핍/적정/발달 판정
- 기둥별 십성 (한글 + 한자)
- 기둥별 십이운성 (한글 + 한자)
- 기둥별 신살 배치 (슬러그·한글·한자)
- 용신·희신·기신 **오행 3종** (어떤 오행인지까지만)
- 일간 신강신약 **7단계 라벨 + position** (극약/태약/신약/중화/신강/태강/극왕)
- 대운 7구간 배열 (천간·지지·오행·startYear·age·hue)

### 차단 유지 (내부 근거·수치·텍스트)

| 필드 | 차단 이유 |
|------|-----------|
| `yongSin.reasoning` | 용신 선택 알고리즘·조합 로직이 드러남 |
| `dayMasterStrength.score` / `analysis` | 신강신약 점수 산정 가중치 단서 |
| `gyeokGuk` (전부) | 격국 판정 기준 노출 위험 |
| `jiJangGan` (전부) | 세력 수치는 십성 가중치의 기초 |
| `branchRelations` 상세 | 삼합/삼형/육해 판정 로직 단서 |
| `wolRyeong` | 득령 판정 근거 (계산엔 사용, 노출은 금지) |
| `tenGodsDistribution` | 지장간 세력 반영 가중치 노출 |
| `specialMarks` / `dominantElements` / `weakElements` | 내부 태그 — 해석엔 영향 없음, 외부엔 의미 없음 |
| PR3 Claude 프롬프트 원문 / 캐릭터 톤 설정 / 해석 텍스트 | 제품의 핵심 IP |
| `school_interpreter` 내부 유파 가중치 | 해석 차별점 원천 |

### 구현 경계

- **유일한 노출 통로**: `src/http/transformers/`. 라우트는 `SajuData` 를 트랜스포머에 넘기고 반환 DTO만 직렬화한다.
- **회귀 방지**: `tests/http/transformers.test.ts` 의 "영업 비밀 필드 부재" 스냅샷 테스트가 내부 필드명·근거 텍스트 원문을 응답에서 찾아 실패시킨다.
- 신규 트랜스포머 추가 시 이 차단/공개 매트릭스를 따른다.
